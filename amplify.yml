version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - amplifyPush --simple
  artifacts:
    baseDirectory: dist/theme
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --max_old_space_size=4096 && npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/theme
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
